# NAMI AutoCap Ephemeral Sandbox

This public repository contains only the sandbox workflow. The AutoCap binary is never committed to GitHub. It is fetched at runtime from the owner's private NAMI storage, scanned, tested on an ephemeral Windows runner, deleted, and only non-binary reports are uploaded.
